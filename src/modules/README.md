# Módulos

Cada módulo mantém as camadas `domain`, `application`, `infrastructure` e
`presentation` quando forem necessárias. A Fase 1 preserva apenas a estrutura;
nenhuma regra ou integração de negócio foi criada.
